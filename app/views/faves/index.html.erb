<div class="row span9">
  <div class="page-header">
    <h3>List of favorites</h3>  
  </div>

  <% if @user_faves.empty? %>
    <div class="alert alert-info">
      You dont have any favorites yet.
    </div>
  <% else %>
	  <%= will_paginate @user_faves %>
    <table class="table " border="0">
      <thead>
        <th>Item</th>
        <th width="95px">Added</th>
        <th width='85px'>Action</th>
      </thead>
      <tbody>
        <% @user_faves.each do |fave| %>
          <tr>
            <td>
              <%= link_to truncate(fave.page_title,length:100), fave.url, target: "_blank" %>
              <br />
              <p class="muted">
              <%= truncate fave.description,length:200 %>
              </p>
            </td>
			<td><%= fave.created_at.strftime("#{fave.created_at.day.ordinalize} %b, %Y ")  %></td>
            <td>
              <%= link_to "Edit", edit_user_fave_path(current_user, fave), class:'btn btn-mini',style:'margin-right:5px;' %><%= link_to "Delete", user_fave_path(current_user, fave),method: :delete,class:'btn btn-mini' %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
	  <%= will_paginate @user_faves %>
  <% end %>
  <div class="well well-small">
    <%= render 'form' %>
  </div>
</div>
